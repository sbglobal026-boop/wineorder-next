import CategoryCards from '@/components/home/CategoryCards'

// 이전 홈(/)의 카드 3개 선택 화면 — 메인이 Top Drop으로 바뀌면서 이 주소로 옮김
// 사이트 메뉴에서는 링크하지 않고 주소 직접 입력으로만 접근
export default function WelcomePage() {
  return <CategoryCards />
}
